<%= form_for(@deadline) do |f| %>
  <% if @deadline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deadline.errors.count, "error") %> prohibited this deadline from being saved:</h2>

      <ul>
      <% @deadline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :yearbook %><br />
    <%= f.text_field :yearbook %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :due_date %><br />
    <%= f.datetime_select :due_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
