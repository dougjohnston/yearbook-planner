<%= form_for(@spread) do |f| %>
  <% if @spread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spread.errors.count, "error") %> prohibited this spread from being saved:</h2>

      <ul>
      <% @spread.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section %><br />
    <%= f.text_field :section %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
