<!DOCTYPE html>
<html>
<head>
  <title>AAI <%= content_for?(:title) ? content_for(:title) : "Yearbook Planner" %> - yearbookplanner.com</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </header>

  <h1><%= content_for :title %></h1>
  <%= yield %>
</body>
</html>
